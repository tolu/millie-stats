node_modules/
dist/
.wrangler/
.dev.vars
.DS_Store
*.log

# Generated by `wrangler types`. Not needed — @cloudflare/workers-types is a
# devDependency instead — but ignored so a stray run does not get committed.
worker-configuration.d.ts

# Tooling context IS tracked (launch.json, settings.json); only personal
# overrides are not.
.claude/settings.local.json
