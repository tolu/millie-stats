{
  "name": "millie-stats",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:worker": "wrangler dev --config dist/server/wrangler.json --persist-to .wrangler/state",
    "test": "vitest run",
    "deploy": "npm run build && wrangler deploy",
    "db:local": "wrangler d1 execute millie --local --file",
    "db:remote": "wrangler d1 execute millie --remote --file"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.121.0",
    "@solidjs/web": "2.0.0-rc.3",
    "solid-js": "2.0.0-rc.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.0.0",
    "@cloudflare/workers-types": "^5.20260827.1",
    "@solidjs/vite-plugin": "3.0.0-next.34",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^3.0.0",
    "wrangler": "^4.100.0"
  }
}
